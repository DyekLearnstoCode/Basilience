<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- LOGO (Parallax) -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="@dimen/header_logo_size"
        android:layout_height="@dimen/header_logo_size"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="@dimen/header_logo_margin_bottom"
        android:src="@drawable/basilience_logo"
        app:layout_collapseMode="parallax"
        app:layout_collapseParallaxMultiplier="0.7" />

    <!-- BACK BUTTON (Pinned) -->
    <ImageView
        android:id="@+id/btnBack"
        android:layout_width="@dimen/back_button_size"
        android:layout_height="@dimen/back_button_size"
        android:layout_margin="@dimen/back_button_margin"
        android:padding="8dp"
        android:src="@drawable/arrow_back"
        android:clickable="true"
        android:focusable="true"
        android:background="@null"
        android:elevation="100dp"
        android:translationZ="100dp"
        android:visibility="gone"
        app:layout_collapseMode="pin" />

    <!-- SETTINGS GEAR (Pinned) -->
    <ImageView
        android:id="@+id/btnGlobalSettings"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="end|top"
        android:layout_margin="16dp"
        android:padding="8dp"
        android:src="@drawable/nav_setting_icon"
        android:clickable="true"
        android:background="@null"
        android:elevation="100dp"
        android:translationZ="100dp"
        app:layout_collapseMode="pin"
        android:contentDescription="@string/nav3" />

</merge>