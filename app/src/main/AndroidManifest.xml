<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/basiliencelogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/basiliencelogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">

        <!-- Launcher Activity -->
        <activity
            android:name=".Auth_Login_Activity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup Activity -->
        <activity
            android:name=".Auth_Register_Activity"
            android:theme="@style/Theme.Basilience" />

        <!-- Main Activity (Dashboard + BottomNav host) -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Basilience"
            android:windowSoftInputMode="adjustPan" />

    </application>
</manifest>